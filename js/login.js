import {supabase} from './config.js';
const form=document.getElementById('loginForm'),message=document.getElementById('message');
form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='Signing in...';const email=document.getElementById('email').value.trim(),password=document.getElementById('password').value,selectedRole=document.getElementById('role').value,destination=document.getElementById('destination').value;const {data,error}=await supabase.auth.signInWithPassword({email,password});if(error){message.textContent=error.message;return}const {data:emp,error:pe}=await supabase.from('employees').select('role,active').eq('auth_user_id',data.user.id).single();if(pe||!emp||!emp.active||emp.role!==selectedRole){await supabase.auth.signOut();message.textContent='Login role does not match your account.';return}window.location.href=destination==='attendance'?'attendance.html':'employee-data.html';});
